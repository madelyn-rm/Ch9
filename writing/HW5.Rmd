---
title: "HW5"
author: "Madelyn Maclaughlin"
date: "2024-11-04"
output: pdf_document
---

```{r setup, include=FALSE, message = FALSE, warning = FALSE}
library(knitr)
knitr::opts_chunk$set(echo = TRUE)
```

Load in the data:
```{r}
library(tidyverse)
homicides <- read.csv('/Users/madelynmaclaughlin/Downloads/R_class/Ch_9/data/homicides.csv')
head(homicides)
```

I am creating the graph from choice 2. Let's try and reduce the data to what we need and organize it.
```{r}
homicides_dates <- homicides %>% 
  select(reported_date, city) %>% 
  filter(city == "Baltimore") %>% 
  mutate(reported_date = ymd(reported_date)) %>% 
  arrange(reported_date)
head(homicides_dates)
```

Sorting data into different groups based on season.
```{r}
summer <- homicides_dates %>% 
  mutate(month = month(reported_date)) %>% 
  filter(month %in% c(6:11)) %>% 
  group_by(reported_date)

winter <- homicides_dates %>% 
  mutate(month = month(reported_date)) %>% 
  filter(month %in% c(1:5, 12)) %>% 
  group_by(reported_date)
```

Begin graphing.
```{r}
ggplot() +
  geom_histogram(summer, mapping = aes(x = reported_date), bins = 80, fill = "lightgray", color = "gray50") +
  geom_histogram(winter, mapping = aes(x = reported_date), bins = 80, fill = "lightblue", color = "gray50") +
  theme_dark()
```












